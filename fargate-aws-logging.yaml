kind: ConfigMap
apiVersion: v1
metadata:
  name: aws-logging
  namespace: aws-observability
data:
  output.conf: |
    [OUTPUT]
        Name cloudwatch_logs
        Match   *
        region eu-west-3
        log_group_name /aws/eks/fargate/orchestration-eks
        log_stream_prefix fargate-
        auto_create_group true
